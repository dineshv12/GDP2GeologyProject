<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s525127.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".secondpage" />
        <activity android:name=".mainpage" />
        <activity android:name=".strikedippage" />
        <activity android:name=".existingproject" />
        <activity android:name=".quadrant" />
        <activity
            android:name=".quadrantgraph"
            android:label="@string/title_activity_quadrantgraph"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>